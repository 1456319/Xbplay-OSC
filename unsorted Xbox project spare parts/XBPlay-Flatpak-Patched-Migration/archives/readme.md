Files in here are generated via npm run `npm run package-flatpak`

The file name will include the version in the package json file

Once created, upload them to server and then update com.studio08.xbplay sha and file name